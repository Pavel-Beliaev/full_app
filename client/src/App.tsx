import { Button } from '@nextui-org/react';

const a = (b: string) => {
  return 1;
};

export const App = () => {

  // a('a');

  return (
    <div>
      <Button color='primary'>Button</Button>
    </div>
  );
};